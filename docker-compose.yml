version: '3.8'

services:
  hiit-timer:
    build:
      context: .
      shm_size: '256m'
    ports:
      - "9012:9012"
    volumes:
      - hiit_data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    mem_limit: 512m
      
volumes:
  hiit_data:
