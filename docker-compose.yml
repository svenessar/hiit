version: '3.8'

services:
  hiit-timer:
    build: .
    ports:
      - "9012:9012"
    volumes:
      - hiit_data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      
volumes:
  hiit_data:
